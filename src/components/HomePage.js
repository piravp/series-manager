import React from 'react';

const HomePage = (props) => {
    console.log(props);
    return (
        <div>
            <p>This is the home page.</p>
        </div>
    );
};




export default HomePage;